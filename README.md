## A Convolution Neural Network (CNN) From Scratch With Python

### Project Structure

```bash
$ pip install -r requirements.txt
```

Then, run it with no arguments:

```bash
$ python cnn.py
$ python cnn_keras.py
```